name: Build App

runs:
  using: composite
  steps:
    - name: Setup Gradle Environment
      uses: JustSiHeshteger/pipeline-action/setup-gradle-env@v1

    - name: Build (without tests)
      shell: bash
      run: ${{ env.WORKING_DIRECTORY }}/gradlew clean build -x test